#include<stdio.h>

void main(){
    int l,h,a;
    printf("Enter the length and height of the triangle :");
    scanf('%d', &h);
    a = (1 / 2) * l * h;
    printf("The area of the triangle is:%d", a);
}